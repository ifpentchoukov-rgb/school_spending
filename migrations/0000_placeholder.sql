-- 0000_placeholder.sql
-- Phase 0 placeholder. Per PLAN.md §6, the directory must exist; the first real
-- schema migration lands in Phase 1 (`0001_core_schema.sql`).
-- Apply via the Supabase MCP `apply_migration` tool, not psql.

SELECT 1;
