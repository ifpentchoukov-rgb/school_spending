# Dependencies for legacy/ scripts (Step 1 + Step 2).
# The new production system in PLAN.md will have its own pyproject.toml.

pandas>=2.0
openpyxl>=3.1
pdfplumber>=0.10

# System dependencies (not pip-installable):
#   - mdbtools  (apt install mdbtools)  — only needed for CA SACS .mdb extraction
#   - unzip                              — for SACS .exe self-extracting archives
#   - curl                               — for downloading source files
