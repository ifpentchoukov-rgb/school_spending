# Copy to .env.local for local development. Never commit the populated file.
# Production values live in GitHub Actions secrets (PLAN.md §3).

# Supabase project: school-budget-tracker (id: bwkgcofsxubdofklpsaw, region: us-east-1)
SUPABASE_URL=https://bwkgcofsxubdofklpsaw.supabase.co
SUPABASE_PUBLISHABLE_KEY=
# Service-role key — required by extractors to write budget_events / source_documents.
# Treat as a secret. Pull from Supabase dashboard (Project Settings → API).
SUPABASE_SERVICE_ROLE_KEY=
